import { Navbar as MantineNavbar } from '@mantine/core';

const Navbar = () => {
	return (
		<MantineNavbar width={{ base: 300 }}>
			<p>I'm navbar</p>
		</MantineNavbar>
	);
};

export default Navbar;
